/**
 * Feature flags.
 *
 * Keep these hardcoded for now (soft launch), and flip them when you're ready.
 * This file is safe to import from both server and client components.
 */
export const features = {
  androidEnabled: false,
} as const;
